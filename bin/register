#!/usr/bin/env bash

BASEDIR=$(dirname $0)
$BASEDIR/target
source $BASEDIR/user
cf create-service-broker service-db-dumper-do-not-use myUs3r myPasssw0rd $SERVICE_URL
cf enable-service-access service-db-dumper -o $AVAILABLE_ON_ORG