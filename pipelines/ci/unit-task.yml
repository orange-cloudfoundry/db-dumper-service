---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: '3.3.9-jdk-8'

inputs:
- name: db-dumper-service

run:
  path: db-dumper-service/pipelines/ci/unit-task.sh